
Find an R package and underlying database
that draws geographic maps of countries in the EU,
provinces within countries,
counties within provinces,
districts within counties,
parishes within ditricts. 

Use Ireland as an example:
3 countries,
4 provences
32 counties 
etc

At the county level, count the number of counties that 
touch the sea (distance zero)
touch a neighbouring county that touches the sea (distance one),
touch a neighbouring county that touches a neighbouring county that the sea (distance two).

Visualise the steps in a dashboard on gh-pages.
Use recent projects vignettes as dashboard templates.
